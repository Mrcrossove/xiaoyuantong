# 开发规则补充

1. 小程序页面样式必须优先使用 `rpx`，用于适配不同手机屏幕尺寸，避免固定 `px` 导致错位、遮挡、输入框过小或内容不可见。
2. 每次涉及后端代码变更时，完成后必须给出本地推送 GitHub 命令，以及云服务器 `git pull / npm install / build / pm2 restart` 等完整拉取重启命令。
3. 如果本次只修改小程序前端，不涉及后端服务端代码，必须明确说明不需要服务器重启，并提示重新编译、预览或上传小程序。
