# 内容管理接口协议

本文档用于对齐以下 3 组真实后端接口：

- `/school/content`
- `/user/publish`
- `/product/spec`

## 1. 统一返回结构

成功响应：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "traceId": "a1b2c3d4"
}
```

失败响应：

```json
{
  "code": 40001,
  "message": "参数错误：pageSize 超出范围",
  "data": null,
  "traceId": "a1b2c3d4"
}
```

约定：

- `code = 0` 表示成功
- `message` 为给前端直接展示的中文提示
- `traceId` 用于后端排查问题
- `data` 在失败时可为 `null`

## 2. 统一分页结构

列表接口 `data` 统一为：

```json
{
  "list": [],
  "page": 1,
  "pageSize": 10,
  "total": 0
}
```

## 3. 批量接口结构

批量删除：

```json
{
  "ids": [1, 2, 3]
}
```

批量状态更新：

```json
{
  "ids": [1, 2, 3],
  "status": "启用"
}
```

## 4. 高校内容统计

### 4.1 列表查询

`GET /school/content`

查询参数：

- `page`
- `pageSize`
- `keyword`
- `status`
- `school`

返回项：

```json
{
  "id": 1,
  "school": "清华大学",
  "userCount": 2360,
  "postCount": 1280,
  "storeCount": 42,
  "orderCount": 1386,
  "gmv": 86520,
  "verifyPassRate": "84.6%",
  "status": "启用"
}
```

### 4.2 新增

`POST /school/content`

### 4.3 编辑

`PUT /school/content/{id}`

### 4.4 单条启停

`PATCH /school/content/{id}/toggle-status`

### 4.5 单条删除

`DELETE /school/content/{id}`

### 4.6 批量删除

`POST /school/content/batch-delete`

### 4.7 批量状态更新

`POST /school/content/batch-status`

## 5. 用户发布记录

### 5.1 列表查询

`GET /user/publish`

查询参数：

- `page`
- `pageSize`
- `keyword`
- `school`
- `type`
- `status`

返回项：

```json
{
  "id": 1,
  "school": "清华大学",
  "user": "林知夏",
  "type": "帖子",
  "title": "清华食堂晚饭拼饭",
  "status": "已发布",
  "createdAt": "2026-04-07 18:26"
}
```

### 5.2 新增

`POST /user/publish`

### 5.3 编辑

`PUT /user/publish/{id}`

### 5.4 单条启停

`PATCH /user/publish/{id}/toggle-status`

### 5.5 单条删除

`DELETE /user/publish/{id}`

### 5.6 批量删除

`POST /user/publish/batch-delete`

### 5.7 批量状态更新

`POST /user/publish/batch-status`

## 6. 商品规格管理

### 6.1 列表查询

`GET /product/spec`

查询参数：

- `page`
- `pageSize`
- `keyword`
- `school`
- `storeName`
- `status`

返回项：

```json
{
  "id": 1,
  "school": "清华大学",
  "storeName": "清园鲜果铺",
  "productName": "鲜切西瓜杯",
  "specGroup": "规格",
  "specValue": "大杯",
  "price": 12.8,
  "stock": 68,
  "status": "启用",
  "updatedAt": "2026-04-08 10:20"
}
```

### 6.2 新增

`POST /product/spec`

### 6.3 编辑

`PUT /product/spec/{id}`

### 6.4 单条启停

`PATCH /product/spec/{id}/toggle-status`

### 6.5 单条删除

`DELETE /product/spec/{id}`

### 6.6 批量删除

`POST /product/spec/batch-delete`

### 6.7 批量状态更新

`POST /product/spec/batch-status`

## 7. 错误码建议

- `0`：成功
- `40001`：参数错误
- `40004`：目标记录不存在
- `40013`：无权限操作当前高校数据
- `50000`：服务器内部错误
